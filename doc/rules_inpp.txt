RULE 1 :
        # La clé d'un PS est nom + prenom + numero + rue1 + cp + commune
        # Si cette clé est présente dans personne_activite ca matche

RULE 2 :
         # La clé d'un PS est nom + prenom + rue1 + cp + commune
         # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 3 :
        # La clé d'un PS est nom + prenom + numero + rue1 + departement + commune
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 4 :
        # La clé d'un PS est nom + numero + rue1 + cp + commune
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 5 :
        # La clé d'un PS est nom + rue1 + cp + commune
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche
        # Jamais activée sur plasticien

RULE 6 :
        # La clé d'un PS est prenom + nom + rue1 (75%) + cp + commune
        # le matching de rue1 est juste à 75%
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 7 :
        # La clé d'un PS est prenom + nom + cp + commune
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 8 :
        # La clé d'un PS est prenom + nom + (numero + rue1 + cp + commune à 75%)
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 9 :
        # La clé d'un PS est nom + cp + commune
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 10 :
        # La clé d'un PS est prenom + nom + dept + commune
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 11 :
        # La clé d'un PS est prenom + nom + dept
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche

RULE 12 :
        # La clé d'un PS est prenom + nom
        # Si cette clé est présente 1 et 1 seule fois dans personne_activite ca matche
        # Activée 1 seule fois sur plasticien et 2 sur pediatre

# Plasticien : [633, 6, 39, 7, 0, 78, 32, 29, 3, 5, 4, 1]
# Pediatre : [1958, 19, 38, 43, 2, 217, 59, 40, 7, 1, 12, 2]




